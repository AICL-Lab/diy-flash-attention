{
  "private": true,
  "name": "diy-flash-attention-root",
  "version": "2.0.0",
  "description": "DIY FlashAttention - Educational FlashAttention Implementation with Triton",
  "scripts": {
    "docs:dev": "cd docs && npm run dev",
    "docs:build": "cd docs && npm run build",
    "docs:preview": "cd docs && npm run preview",
    "docs:install": "cd docs && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
